{
  "name": "school-site-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API for school site CMS backend",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
